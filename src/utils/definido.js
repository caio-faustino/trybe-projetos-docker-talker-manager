// Define uma função chamada 'definido' que verifica se um valor está definido
const definido = (value) => value || value === 0;

// Exporta a função 'definido' para ser utilizada em outros arquivos
module.exports = definido;
